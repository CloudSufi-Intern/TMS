package cloudsufi.nextgen.tms.enums;
import cloudsufi.nextgen.tms.entity.UserEntity;

/**
 * Defines the organizational roles available within the Task Management System.
 * These roles determine the permissions and responsibilities assigned to a {@link UserEntity }.
 * @author Ansh Parnami
 */
public enum Role {
    IT, ENGINEERING, HR, MANAGER, LEAD, ARCHITECT, DEVOPS, DEVSECOPS, INTERN
}
